# Lessons log

Read this at the start of Phase 3 (art direction). Every entry came from a user correction; each one changed a rule somewhere in this skill. Append, never prune — a lesson that stops appearing in the output still belongs here.

| Date | Project | What went wrong | Root cause | What changed in the skill |
| --- | --- | --- | --- | --- |
| 2026-09-07 | Brandigade (brandigade.com → Astro) | First direction used paper surfaces, Instrument Serif with italic emphasis, mono uppercase eyebrows, ink chapters and a green accent. The user recognised it as the same look the skill had produced for their other site (brio3.com) and asked for the theme to come from the logo's blues with "a unique touch". | The audit noted the logo but never sampled its colours; art direction chose a palette from taste; nothing compared the direction with the skill's earlier outputs for the same user. | Commitment 3 in `SKILL.md`; "The skill's own house style" in `anti-patterns.md`; logo sampling in `audit.md`; logo-first prompts, colour and typography rules and the house-recipe check in `art-direction.md`; "Logo colours sampled" and "Distance from the house recipe" fields in `templates/DESIGN.md`. |
| 2026-09-07 | Brandigade | JetBrains Mono was used for labels, times and dial counts. The user: "does not look like a suitable font, if you know what I mean" — it read as a code editor on a sales firm's site. | Monospace had become a reflex for "data" regardless of the company's voice. | Typography rule in `art-direction.md` (mono only for developer/infrastructure products; tabular figures in the sans otherwise); mono bullet in the house-style anti-pattern. |
| 2026-09-07 | Brandigade | A decorative SVG mark passed a class from its parent rendered as a 980px block in normal flow in three places; the first v2 render was 2,000px taller than intended. | Astro scoped styles do not reach child-component markup; the render was trusted before being looked at. | Rendering trap added to `visual-qa.md`. |
| 2026-09-07 | Brandigade | The user asked for the skill to learn from corrections automatically. | The skill had no feedback loop: corrections fixed the project but never the process. | "Learning from corrections" section in `SKILL.md` and this log. |
| 2026-09-07 | Brandigade | The Intent Finder's result card had a coloured left border for its heat (hot/warm/cool); the user called it "a Claude default website design gimmick". The same stripe device had spread to the "most popular" plan, the comparison column and a closing rule. | Status was encoded as a decorative stripe instead of as data; the anti-pattern list had no entry for it. | "The accent-stripe card" added to `anti-patterns.md`; status now shown as a labelled tag with a data-coloured mark. |
| 2026-09-08 | Brandigade | The process chapter used a thin rule with mark icons at each step; the user called it a progress bar. | A timeline drawn as a stepper is UI chrome, not editorial design; the anti-pattern list covered cards but not steppers. | "The stepper timeline" added to `anti-patterns.md`; the component became a ruled schedule with the time as the dominant number. |
| 2026-09-08 | Brandigade | The hero background — flat white with a lone hairline watermark — was called "not that great". | The hero object had no ground of its own; the watermark alone was carrying the composition. | Hero-visual ground rule added to `anti-patterns.md`; the calendar now sits on a sky panel with a faint calendar grid and the layered mark. |
| 2026-09-08 | Brandigade | After the stepper was replaced with ruled columns, the user asked for "an actual timeline component, not grid" and for the site to have "many beautiful components, design graphics". | The fix to the stepper had removed the timeline instead of designing one; the direction had no graphics layer at all — only type, rules and data fragments. | Anti-patterns for the grid-as-timeline and the graphics-free site; imagery rule in `art-direction.md`; the project got a drawn timeline with milestone art, spot illustrations and glyphs. |
| 2026-09-08 | Brandigade | The user reported the graphics were "broken a little bit". Element-level inspection found: city labels clipped by the diagram's viewBox and colliding with the hub label, the hero watermark spilling out of its sky panel onto white, a play glyph overflowing its circle, a confirmation stamp covering booked blocks, a wrench drawn in two pieces that never met, and stray marks floating beside two illustrations. | The graphics layer had only ever been reviewed in full-page captures, where none of these are visible; the watermark was positioned against a `::before` background that could not clip it. | "Custom artwork and diagrams" section in `anti-patterns.md`; "Inspecting custom artwork" pass in `visual-qa.md`. |
| 2026-09-08 | Brandigade | Fixing the escaping watermark introduced two new faults: clipping it inside a `-100vw` full-bleed ground pushed it 100vw off-screen so it vanished, and the Intent Finder's mark was cropped to a diagonal wedge crossing the headline. | The first fix was verified by asserting `overflow: hidden` rather than by looking at the render; a clipped decoration needs a judgement about what remains visible, not just a containment rule. | Two entries in the "Custom artwork and diagrams" anti-patterns: the meaningless-wedge watermark and positioning inside a full-bleed container. |
| 2026-09-08 | CleoHR Passport (front-door prototype, Astro) | The direction avoided monospace but set every label, stamp, chip, tab and field in Archivo condensed, tracked uppercase, plus an MRZ strip on the Passport card. The user: "can we not use the 'code' kinda font here, not very suitable". | The mono rule was treated as a font-family rule; the *voice* — tracked small capitals in a hard grotesk, and strings that look like machine output — was not covered. The direction also chose a condensed industrial face for a people product on the strength of the "document" concept. | Typography rules in `design-theory.md` §2; two anti-patterns ("Tracked capitals in a condensed or geometric grotesk" and "Typesetting an object that looks like machine output"); the typography paragraph in `art-direction.md` now names it. |
| 2026-09-08 | CleoHR Passport | The user asked for a new logo, and for the skill to "learn about design/colour theory" and apply it. The first palette used orange as three full-bleed fields on one page, so the primary button had to turn navy on those chapters, and every label and numeral took the accent. | The skill had no written colour theory: no proportion rule, no scale-building method, no contrast scale beyond WCAG, no logo rules. | New `references/design-theory.md` (harmony, 60/30/10, OKLCH scales, 12-step roles, WCAG + APCA, typography scale and caps rules, Gestalt, logo construction, a palette checklist); hooked from `SKILL.md`, `art-direction.md` and `templates/DESIGN.md`; anti-pattern for multiple accent fields / changing action colour. |
